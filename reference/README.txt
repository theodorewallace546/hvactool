Put the two source reference images here:

1. The HVAC training booklet sizing page (tonnage/sqft/BTU tables, mini-split
   BTU table, "how to find BTU/tonnage/age of existing equipment" section).
2. The duct CFM reference chart (flexible round duct sizes vs. design
   airflow).

Claude could not copy these in automatically — macOS blocked filesystem
access to the Messages Attachments folder they were shared from. Drag them
in from Messages/Photos manually.

Their content has already been transcribed into
../knowledge/generic_hvac_knowledge.md — the images themselves are just kept
here as the source of truth in case the knowledge file needs to be checked
or extended later (e.g. the rectangular duct chart that was cut off in the
original screenshot, or the ambiguous "divide by 3" handwritten note flagged
at the top of that file).
